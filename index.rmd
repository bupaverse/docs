---
title: "bupaR Docs"
---

```{r include = F}
library(bslib)
library(htmltools)
library(shiny)
library(bupaverse)
library(tidyverse)
```

```{r xaringanExtra-clipboard, echo=FALSE}
xaringanExtra::use_clipboard()
```

<style>
  .title{
    display: none;
  }
</style>

<br/>
<br/>


```{r echo = F, out.width = "50%", fig.align = "right"}
knitr::include_graphics("images/logo/docs.jpg")
```




```{r echo = F, message = F, warning = F}
library(readr)
library(purrr)

read_csv("cards.csv") %>%
    mutate(card = pmap(list(class, head, body, icon), ~card(card_header(class = ..1, ..2), 
                                                       card_image(..4)))) -> cards

layout_column_wrap(cards$card[1], cards$card[2], cards$card[3],
                   cards$card[4], cards$card[5], width = "150px")
```



